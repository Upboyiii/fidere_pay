# NextAuth 配置
# 应用 URL（生产环境需要改为实际域名）
NEXTAUTH_URL=http://localhost:3000
# 生产环境示例：
# NEXTAUTH_URL=https://www.fidere.xyz

# NextAuth Secret（用于加密 JWT）
# 生产环境务必修改为强随机字符串！
NEXTAUTH_SECRET=fidere-pay-nextauth-secret-key-2026-change-in-production

# 注意：NextAuth 现在使用标准的 /api/auth 路径
# 不需要配置 NEXTAUTH_BASEPATH

# 其他配置（根据需要添加）
# CUSTOM_KEY=your_custom_key
# SITE_TYPE=admin
# proxy=
